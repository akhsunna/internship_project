$("ul.errors").html("")
<% if @book.errors.any? %>
<% @book.errors.full_messages.each do |message| %>
$("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
<% end %>
<% else %>

$("#modals").modal("hide");
location = "<%= user_path(current_user.id) %>";
<% end %>