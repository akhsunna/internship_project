.modal-dialog
  .modal-content
    .modal-header
      %button.close{'data-dismiss' => 'modals', type: 'button'}
        %span{'aria-hidden' => 'true'} ×
        %span.sr-only Close
      %h4.modal-title Genres
    .modal-body
      .well{ style: 'max-height:300px;overflow:auto;' }
        %ul.list-genre.checked-list-box
          -#- Genre.all.sort_by{|obj| obj.name}.each do |genre|
          -#  - if @genres.include?(genre)
          -#    = link_to method: :put, remote: true, data: { id: genre.id } do
          -#      %li.list-genre-item{'data-checked' => 'true'}= genre.name
          -#  -else
          -#    = link_to method: :put, remote: true, data: { id: genre.id } do
          -#    %li.list-genre-item= genre.name
          - Genre.all.each do |genre|
            %li.list-genre-item{'data-checked' => 'true'}= genre.name
    .modal-footer